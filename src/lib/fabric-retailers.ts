import type { FabricRetailerLink } from "@/lib/fabric-types";

/**
 * Intentionally empty for the validation MVP. Add only reviewed, real destinations.
 * The interface must stay hidden while this list has no approved records.
 */
export const approvedFabricRetailerLinks: FabricRetailerLink[] = [];
